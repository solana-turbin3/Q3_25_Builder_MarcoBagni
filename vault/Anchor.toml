[toolchain]
package_manager = "yarn"

[features]
resolution = true
skip-lint = false

[programs.devnet]
vault = "Ej8tPv7YQqJAxuscLC8qZnWkynLZwm7QpaT5fSJxQz6Y"

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "devnet"
wallet = "~/.config/solana/d1x.json"

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"
deploy = "bash scripts/deploy.sh"
run-tests = "bash scripts/test.sh"
init = "yarn ts-node scripts/init-vault.ts"
deposit = "yarn ts-node scripts/deposit.ts"
withdraw = "yarn ts-node scripts/withdraw.ts"